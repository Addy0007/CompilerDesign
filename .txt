%{
#include <stdio.h>
#include <math.h>
%}

%option noyywrap

%%
[0-9]+ {
    double num1 = atof(yytext);
    double num2 = atof(yytext);
	
    double Value = num1+num2;
    printf("Sum of %f and %f is %f\n", num1, num2, Value);
}

. ;

%%

int main() {
    yylex();
    return 0;
}
